#pragma once

#include "project_data.hpp"

namespace PrismCascade {

/*
class ProjectLoader {
public:
    ProjectLoader();
    ~ProjectLoader();

    Project load_project(const std::string& filePath);

private:
    // TODO: parse, validate, etc...
};
*/

}

